# PostgreSQL
POSTGRES_DB=mimicry
POSTGRES_USER=mimicry
POSTGRES_PASSWORD=changeme

# pgAdmin (http://localhost:5050)
PGADMIN_DEFAULT_EMAIL=admin@mimicry.local
PGADMIN_DEFAULT_PASSWORD=changeme

# App (автоматически собирается из PostgreSQL переменных выше)
# DATABASE_URL=postgresql://mimicry:changeme@db:5432/mimicry

# Датасеты (монтируется в Docker)
DATASET_PATH=/data/datasets
